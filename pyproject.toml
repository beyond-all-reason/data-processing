[project]
name = "data-processing"
version = "0.1.0"
description = " SQL pipelines for processing BAR data"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dbt-core>=1.10.2",
    "dbt-duckdb>=1.9.3",
    "duckdb>=1.3.1",
    "pre-commit>=4.2.0",
    "sqlfluff>=3.4.1",
]
